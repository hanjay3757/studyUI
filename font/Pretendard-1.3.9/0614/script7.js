// var exchangeRate = 1200;
// var korAmount = prompt("환줜 입력", "1만원단위 입력");
// alert("현재 환율 기준" + "$" + korAmount / exchangeRate + "입니다");

var fristNum = prompt("첫번째 숫자 입력", "정수로");
var secondtNum = prompt("두번째 숫자 입력", "정수로");

alert("결과는:" + (Number(fristNum) + Number(secondtNum)));
